@model IEnumerable<Librarian.Models.Publisher>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section Content
{
    <div class="panel">
        <div class="panel-division">
            <a asp-action="Create"><i class="fas fa-plus"></i></a>
        </div>
        <div class="panel-division">
            <table>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td class="flex">
                            <a class="ml-a" asp-action="Edit" asp-route-id="@item.PublisherId">Edit</a>
                            <a class="ml-4" asp-action="Details" asp-route-id="@item.PublisherId">Details</a>
                            <a class="ml-4" asp-action="Delete" asp-route-id="@item.PublisherId">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
}